% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/om-projection.R
\name{project_trajectory}
\alias{project_trajectory}
\title{Project Biomass Trajectory}
\usage{
project_trajectory(
  B_initial,
  catch_series,
  om_config,
  n_years = NULL,
  process_noise = NULL,
  bias_correction = TRUE,
  seed = NULL
)
}
\arguments{
\item{B_initial}{Numeric scalar or vector. Initial biomass per area.}

\item{catch_series}{Numeric vector or matrix. Catch time series. If a
vector, used for all areas (single-area) or recycled. If a matrix,
rows are years and columns are areas.}

\item{om_config}{An \code{\link{om_config}} object.}

\item{n_years}{Integer. Number of years to project (derived from
\code{catch_series} if not specified).}

\item{process_noise}{Logical. Whether to add process noise. Default
\code{TRUE} if \code{sigma_process} is in \code{true_params}.}

\item{bias_correction}{Logical. Whether to apply lognormal mean bias
correction (\eqn{-\sigma_p^2/2}) when process noise is enabled.
Default \code{TRUE}.}

\item{seed}{Integer random seed, or \code{NULL}.}
}
\value{
A matrix of biomass with \code{n_years + 1} rows (including
the initial year) and \code{n_areas} columns. Row 1 is the initial
biomass.
}
\description{
Run a multi-year forward projection of the operating model.
}
\examples{
\dontrun{
cfg <- om_config(
  n_areas = 1,
  true_params = list(
    r = 0.3, K = 5000, m = 2,
    sigma_obs = 0.2, q = 1e-4, B_initial = 5000
  )
)
catches <- rep(200, 10)
traj <- project_trajectory(5000, catches, cfg, seed = 42)
}

}

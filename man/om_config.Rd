% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{om_config}
\alias{om_config}
\title{Configure the Operating Model}
\usage{
om_config(
  n_areas = 1L,
  movement_rate = 0,
  distance_matrix = NULL,
  attractiveness = NULL,
  decay = 0,
  true_params = NULL
)
}
\arguments{
\item{n_areas}{Integer >= 1. Number of spatial areas (default 1 = non-spatial).}

\item{movement_rate}{Numeric in [0, 1]. Fraction of biomass redistributed per
time step (default 0 = no movement).}

\item{distance_matrix}{Square symmetric matrix of inter-area distances (km),
or \code{NULL} for single area. Diagonal must be zero.}

\item{attractiveness}{Numeric vector of length \code{n_areas} giving habitat
quality weights, or \code{NULL} (all areas equal).}

\item{decay}{Numeric >= 0. Distance decay parameter for the gravity movement
kernel (default 0).}

\item{true_params}{Named list of true parameter values containing at minimum
\code{r}, \code{K}, \code{m}, \code{sigma_obs}, \code{q}, \code{B0}.
\code{q} and \code{B0} may be scalar or per-area vectors. If \code{NULL},
parameters must be supplied separately.}
}
\value{
An S3 object of class \code{om_config}.
}
\description{
Create an operating model configuration specifying the "true" population
dynamics for MSE simulations.
}
\examples{
# Single-area operating model
om <- om_config(
  true_params = list(
    r = 0.3, K = 5000, m = 2,
    sigma_obs = 0.2, q = 1e-4, B0 = 4000
  )
)

# Multi-area with movement
om3 <- om_config(
  n_areas = 3,
  movement_rate = 0.1,
  distance_matrix = matrix(c(0, 100, 200, 100, 0, 100, 200, 100, 0), 3, 3),
  attractiveness = c(1, 1.2, 0.8),
  decay = 0.01,
  true_params = list(
    r = 0.3, K = 5000, m = 2,
    sigma_obs = 0.2, q = rep(1e-4, 3),
    B0 = c(2000, 2500, 1500)
  )
)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/om-projection.R
\name{project_biomass}
\alias{project_biomass}
\title{Project Biomass One Year Forward}
\usage{
project_biomass(
  biomass,
  catch,
  om_config,
  movement_kernel = NULL,
  process_noise = NULL,
  process_state = NULL,
  return_process_state = FALSE,
  seed = NULL
)
}
\arguments{
\item{biomass}{Numeric scalar or vector. Current biomass per area.}

\item{catch}{Numeric scalar or vector. Realized catch per area (same
length as \code{biomass}).}

\item{om_config}{An \code{\link{om_config}} object containing true
population parameters.}

\item{movement_kernel}{Optional pre-computed movement kernel matrix from
\code{\link{build_movement_kernel}}. If \code{NULL} and
\code{om_config$n_areas > 1}, it will be computed from the config.}

\item{process_noise}{Logical. Whether to add stochastic process noise.
Default \code{TRUE} if \code{sigma_process} is present in
\code{true_params}.}

\item{process_state}{Optional previous process-error state for AR1 noise.
Use \code{NULL} for iid process error or the initial step in a series.}

\item{return_process_state}{Logical. If \code{TRUE}, return both biomass
and the updated process-error state.}

\item{seed}{Integer random seed, or \code{NULL}.}
}
\value{
A numeric vector of biomass per area at \eqn{t + 1}.
}
\description{
Advance the operating model biomass state by one time step using the
Pella-Tomlinson surplus production model, with optional process noise
and spatial movement.
}
\details{
The deterministic Pella-Tomlinson update is:
\deqn{B_{t+1,a} = B_{t,a} + P(B_{t,a}) - C_{t,a}}
where
\deqn{P(B) = \frac{r \cdot B \cdot \bigl(1 - (B/K_a)^{m-1}\bigr)}{m}}

For multi-area models, \eqn{K} is distributed across areas proportional
to initial biomass \eqn{B_0}.

If \code{sigma_process > 0}, log-scale process noise is added:
\deqn{B_{t+1,a} = (B_{t,a} + P(B_{t,a}) - C_{t,a}) \cdot
  \exp(\varepsilon_a - \sigma_p^2/2)}
where \eqn{\varepsilon_a \sim N(0, \sigma_p^2)}.

Spatial redistribution (when \code{movement_rate > 0}):
\deqn{B'_a = (1 - \rho) \cdot B_a + \rho \cdot \sum_b K_{ab} \cdot B_b}
where \eqn{K} is the gravity movement kernel.
}
\examples{
\dontrun{
cfg <- om_config(
  n_areas = 1,
  true_params = list(
    r = 0.3, K = 5000, m = 2,
    sigma_obs = 0.2, q = 1e-4, B0 = 5000
  )
)
B1 <- project_biomass(5000, 200, cfg, seed = 1)
}

}
\references{
Pella, J. J.; Tomlinson, P. K. (1969). A generalised stock production model. Inter-American Tropical Tuna Commission Bulletin 13, 419-496.

Turchin, P. (1998). Quantitative Analysis of Movement: Measuring and Modelling Population Redistribution in Animals and Plants. Sinauer Associates, Sunderland, MA.

Biomass is floored at a small positive value (0.01) to prevent
numerical issues.
}
